## 已发现的“明显不完整”后端点位（抽样扫描结果）
- carbon-assets
  - 区块链/上链相关大量 stub（直接 return null/空方法体）：CarbonBlockChainMsgProduce.java
  - 项目文档/内容相关存在 return null/注释掉实现：CarbonMetaregistryDocServiceImpl.java、CarbonMetaregistryDocContentServiceImpl.java、CarbonMetaregistryEsController.java
  - 面板统计固定返回 0（占位）：CarbonCreditAssetsController#assetsTotal
- carbon-system
  - H5 文章/搜索接口 Controller 有，但 Service 全 return null（且完整实现被注释）：CarbonH5ArticleController.java + CarbonH5ArticleServiceImpl.java
- carbon-auth
  - 权限校验 checkPermission 基本放行（缺少按 URL 的权限匹配逻辑）：LoginServiceImpl.java

## 目标
- 像你要求的“同理检查所有后端”：
  1) 列出所有“缺实现/空壳/假返回/注释掉关键逻辑”的位置
  2) 以“前端实际调用的接口”为优先级补齐到最小可用（不再 500/不再无数据）
  3) 最终形成一套可重复执行的冒烟验证（确保后续不回退）

## 扫描与对照方法（全量、可重复）
1. **静态扫描规则（全后端代码）**
   - 搜索模式：空方法体、return null/true/false、TODO/FIXME、NotImplemented、大段注释掉实现、Controller 返回硬编码数据。
   - 产出：按模块（auth/system/assets/trade/gate）输出“问题清单 + 文件路径 + 问题类型 + 影响接口”。

2. **前后端接口对照（以页面为准）**
   - 从 carbon_front/src/api/*.js 抽取所有 url/method
   - 从后端所有 Controller 抽取 mapping
   - 产出：
     - 前端有调用但后端完全找不到的接口（缺端点）
     - 后端有端点但返回空/硬编码/Service 为 stub（缺实现）

3. **运行时冒烟验证（最小用例）**
   - 对每个模块核心接口（getPageList/info/add/update/delete/关键流程）跑一遍：
     - HTTP 状态必须是 200
     - 业务 code 正确（未登录=401，其他=200；不能 500）
     - 列表接口至少返回结构完整（records/total/current）

## 补齐策略（按优先级）
1. **P0：页面必需接口**
   - 优先补齐会导致“页面空白/报错”的接口：getPageList、info、基础字典/菜单、项目库/文档、询报价/履约、资产列表。

2. **P1：关键业务断点**
   - 比如“碳数据报送 submitted -> 上链/审批链路”等：目前在 CarbonBlockChainMsgProduce 中是 return null 的断点，需要补成可运行（必要时做降级：链上不可用时返回明确失败码，而不是 NPE/500）。

3. **P2：统计/面板占位数据**
   - 将固定 0 的统计接口改为真实查询或可解释的降级结果。

## 交付物
- 后端不完整清单（按模块/文件分组）
- 已补齐的接口与影响页面列表
- 冒烟验证脚本/用例（后续一键验证）

## 我接下来会做的实际改动（等你确认后执行）
- 先对 carbon-system 的 H5 文章、carbon-assets 的项目文档/ES 内容、carbon-assets 的上链 producer、carbon-auth 的权限校验四块按 P0/P1 优先补齐。
- 同时补一套接口冒烟验证（确保你点页面不再出现“空白/没数据/500”）。